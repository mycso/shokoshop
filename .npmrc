registry=https://registry.npmjs.org/
# Project-level .npmrc to avoid global/user registry overrides (e.g. Apple's private registry)
# Remove this file if you intentionally want to use a different registry.
